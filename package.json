{
  "name": "typeorm-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "typeorm:migration:create": "(rm -rf dist || :) && npm run typeorm -- migration:create -n",
    "typeorm:migration:generate": "(rm -rf dist || :) && npm run typeorm -- migration:generate -n",
    "typeorm:migration:run": "(rm -rf dist || :) && npm run typeorm -- migration:run",
    "typeorm:migration:revert": "(rm -rf dist || :) && npm run typeorm -- migration:revert"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JoseCToscano/typeorm-playground.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/JoseCToscano/typeorm-playground/issues"
  },
  "homepage": "https://github.com/JoseCToscano/typeorm-playground#readme",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "typeorm": "^0.3.17"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
